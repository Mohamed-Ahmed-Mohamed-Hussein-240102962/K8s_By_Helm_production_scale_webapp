apiVersion: v1
kind: Service
metadata:
  name: {{.Values.backendService.name}}
spec:
  type: {{.Values.backendService.type}}
  selector:
    app: laravel-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9000